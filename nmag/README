To build this documentation you should install Sphinx, for example:

sudo easy_install sphinx

And the following other packages (useful for generating the formulas):

texlive-latex-extra
dvipng

For generating the PDF you may need (this is for Ubuntu):

texlive-fonts-recommended

